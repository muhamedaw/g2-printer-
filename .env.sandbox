# ════════════════════════════════════════════════
# MONEY PRINTER G2 — SANDBOX ENVIRONMENT
# Minimal config — no wallet, no paid APIs needed
# ════════════════════════════════════════════════

# ── Database ──────────────────────────────────
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=mpg2
POSTGRES_USER=mpg2_user
POSTGRES_PASSWORD=change_this_32_char_minimum_password_here

# ── Redis ─────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=change_this_redis_password

# ── Telegram Bot (optional but recommended) ───
TELEGRAM_BOT_TOKEN=get_from_botfather_on_telegram
# HOW: Open Telegram → @BotFather → /newbot → copy token here

# ── App ───────────────────────────────────────
NODE_ENV=development
LOG_LEVEL=info

# ── FREE APIs — NO KEY NEEDED ─────────────────
# DexScreener: api.dexscreener.com (auto-used)
# RugCheck:    api.rugcheck.xyz    (auto-used)
